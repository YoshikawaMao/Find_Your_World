<div class="container">
  <div class="row">
    <div class="col mx-auto">
      <table>
        <% @genres each do |genre| %>
          <tr>
            <td>
              <%= link_to genre.name, admin_anime_path(anime.id) %>
            </td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>